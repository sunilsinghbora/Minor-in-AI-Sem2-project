streamlit>=1.36
pandas>=2.0
numpy>=1.24
yfinance>=0.2.40
scikit-learn>=1.3
plotly>=5.18
requests>=2.31
streamlit-searchbox==0.1.23
# TensorFlow is optional and large; leaving it out by default avoids build failures
# If you need neural models on Cloud/local, uncomment the following line and
# ensure your environment has enough resources to install the wheel.
# tensorflow-cpu==2.20.0
